'use strict';
// В этом файле тоже ничего не нужно менять

/**
 * Вывод текста на экран часов
 *
 * @param {String} text - строка, которая будет отправлена на экран часов
 */
module.exports = function print(text) {
    console.log(text);
};